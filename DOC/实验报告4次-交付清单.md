# 实验报告4次交付清单

## PDF原始要求

根据 `实验报告4次.pdf`，本次实验报告必须覆盖以下四部分，并且每个软件包下的每个类都要截图贴出全部代码：

1. 数据库设计与实现  
   需要截图 `entity` 软件包下面每个类的所有代码
2. Repository 层数据访问实现  
   需要截图 `repository` 软件包下面每个类的所有代码
3. Service 层业务逻辑实现  
   需要截图 `service` 软件包下面每个类的所有代码
4. Controller 层接口实现与前后端联调  
   需要截图 `controller` 软件包下面每个类的所有代码

## 建议报告结构

1. 项目简介
2. 数据库设计与实现
3. Repository 层实现
4. Service 层实现
5. Controller 层实现与前后端联调
6. 系统运行效果截图
7. 总结

## 代码截图清单

### 1. Entity 层

路径：`medicare-server/src/main/java/com/medicare/entity`

需要截图的类：

1. `Department.java`
2. `Doctor.java`
3. `InventoryLog.java`
4. `MedicalRecord.java`
5. `Medicine.java`
6. `OperationLog.java`
7. `Patient.java`
8. `Prescription.java`
9. `PrescriptionItem.java`
10. `Registration.java`
11. `Schedule.java`
12. `SysUser.java`

### 2. Repository 层

路径：`medicare-server/src/main/java/com/medicare/repository`

需要截图的类：

1. `DepartmentRepository.java`
2. `DoctorRepository.java`
3. `InventoryLogRepository.java`
4. `MedicalRecordRepository.java`
5. `MedicineRepository.java`
6. `OperationLogRepository.java`
7. `PatientRepository.java`
8. `PrescriptionItemRepository.java`
9. `PrescriptionRepository.java`
10. `RegistrationRepository.java`
11. `ScheduleRepository.java`
12. `SysUserRepository.java`

### 3. Service 层

路径：`medicare-server/src/main/java/com/medicare/service`

需要截图的类：

1. `DashboardService.java`
2. `DepartmentService.java`
3. `DoctorService.java`
4. `MedicalRecordService.java`
5. `MedicineService.java`
6. `OperationLogService.java`
7. `PatientService.java`
8. `PrescriptionService.java`
9. `RegistrationService.java`
10. `ScheduleService.java`
11. `SysUserService.java`

### 4. Controller 层

路径：`medicare-server/src/main/java/com/medicare/controller`

需要截图的类：

1. `AuthController.java`
2. `DashboardController.java`
3. `DepartmentController.java`
4. `DoctorController.java`
5. `MedicalRecordController.java`
6. `MedicineController.java`
7. `PatientController.java`
8. `PrescriptionController.java`
9. `RegistrationController.java`
10. `ScheduleController.java`
11. `SystemController.java`
12. `UserController.java`

## 前后端联调建议截图

建议在“Controller 层接口实现与前后端联调”章节附上这些运行界面截图：

1. 登录页
2. 运营总览页
3. 基础数据页
4. 挂号预约页
5. 医生工作站页
6. 病历管理页
7. 药品库存页
8. 处方管理页
9. 系统设置与运营中心页

## 当前版本可说明的商用增强点

本次迭代后，可以在报告里重点说明以下“商用化”能力：

1. 登录态校验与角色权限控制
2. 用户密码 BCrypt 加密
3. 登录失败锁定机制
4. 最近登录时间与登录 IP 记录
5. 操作审计日志
6. 系统安全概况看板
7. 统一运营后台界面与角色化导航
8. 患者、挂号、病历、库存、处方等业务模块联动

## 交付前自查

1. 所有上述 Java 类均完成截图
2. 截图顺序与报告目录一致
3. 前后端联调截图能展示系统已正常运行
4. 报告中明确说明新增的商用化能力
5. 报告中说明本地运行端口：
   - 前端：`5173`
   - 后端：`8080`
   - 本地 MySQL：`3307`
